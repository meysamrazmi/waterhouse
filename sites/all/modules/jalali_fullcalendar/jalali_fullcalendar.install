<?php

function jalali_fullcalendar_install(){
    db_update('system')
            ->fields(array(
                'weight' => '-1',
                ))
            ->condition('type','module','=')
            ->condition('name','jalali_fullcalendar','=')
            ->execute();    
}